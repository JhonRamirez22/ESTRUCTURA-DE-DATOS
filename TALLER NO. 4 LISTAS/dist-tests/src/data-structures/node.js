/**
 * Generic node for singly linked list.
 * Each node holds a value and a reference to the next node.
 */
export class Node {
    constructor(value) {
        this.value = value;
        this.next = null;
    }
}
